
# Generated by XcodeGen from project.yml — never committed. A committed copy
# silently drifted four phases behind project.yml (AppDelegate.swift was
# missing from it entirely, so the app would not link), and a CI staleness
# check proved unworkable because XcodeGen's output varies by tool version.
# Generate it with `xcodegen generate`; CI does the same before building.
Verdery.xcodeproj/
