{
  "name": "@verdery/geometry-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical local-planar geometry semantics shared by the backend and both clients",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@verdery/test-fixtures": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
