# Local documentation tooling state (not part of the documentation itself)
docs/.dde/

# OS files
.DS_Store

# Node
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Next.js
.next/
out/

# Playwright
test-results/
playwright-report/
blob-report/

# Swift / Xcode
.build/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/

# Environment
.env
.env.local
.env.*.local

# Editor/tool-generated local artifacts — never commit
.claude/
docs/.claude/
docs/graph-architecture.md

# Apple signing credentials — a .p8 App Store Connect key is a real private
# key. Keep it outside the repository entirely; this is a backstop, not the
# storage location.
*.p8
